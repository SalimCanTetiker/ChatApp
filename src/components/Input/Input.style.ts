import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontWeight: "bold"
    },
    textInput: {
        borderBottomWidth: 1,
        padding: 10,
        margin: 10
    }
})

export default styles